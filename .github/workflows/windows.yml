name: Windows

on: workflow_dispatch

jobs:
  build:

    runs-on: windows-latest
    timeout-minutes: 9999999999

    steps:
    - name: Connect Ngrok
      run: D:\a\FreeVPS\FreeVPS\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
    - name: Enable RDP Connect
      run: | 
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
    - name: Create Ngrok Tunnel
      run: Start-Process Powershell -ArgumentList '-Noexit -Command "D:\a\FreeVPS\FreeVPS\ngrok\ngrok.exe tcp 3389"'
    - name: Connect Windows VM
      run: cmd /c D:\a\FreeVPS\FreeVPS\start.bat
    - name: Keep Action Running
      run: cmd /c D:\a\FreeVPS\FreeVPS\loop.ps1
